__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.DS_Store
.claude/
build/
dist/
/assets/
/examples/
/tutorials/
docs/superpowers/

# benchmark simulation scripts now live in their own standalone repos --
# see the "Full write-up" link in each docs/benchmark/*/*.md page.

# example/showcase run output written to repo root — regenerate via the
# scripts in examples/, don't commit ad hoc; move curated ones to assets/images/
/*.png
/*.txt

# Sphinx build output (Read the Docs builds this fresh)
docs/_build/
